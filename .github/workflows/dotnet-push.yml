name: .NET Build and Test (Push)

on:
  push:
    branches:
      - 'feature/**'
      - 'hotfix/**'
      - 'bugfix/**'
    paths:
      - 'src/**'
      - 'tests/**'
      - '.github/workflows/dotnet.yml'
  workflow_dispatch:

jobs:
  push-test:
    uses: ./.github/workflows/dotnet.yml
